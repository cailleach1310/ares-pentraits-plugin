  <div role="tabpanel" class="tab-pane" id="pentraits">
     <div class="cg-tip"><AnsiFormat @text="This is the place where you can configure traits. You can spend upto {{char.custom.cg_traits_max_points}} points, but you aren't required to spend any of these points at all, this would leave the traits for the character perfectly balanced. <br><br>Please note that raising one trait will lower the opposing trait." /></div>
     <hr>
     {{#if char.custom.cg_traits_set}}
        <p><b>Available Trait Points:</b> {{this.availableTraitPoints}} / {{char.custom.cg_traits_max_points}}</p>
        {{#each char.custom.pen_traits as |trait|}}
        <hr><PenTraitsBox @name={{trait.name}} @rating={{mut trait.rating}} @opposite={{trait.opposite}} @opp_rating={{mut trait.opp_rating}} @maxRating=20 @availPoints={{mut this.availableTraitPoints}} @updated={{action "traitChanged"}}>
        </PenTraitsBox>
        {{/each}}<hr>
    {{else}}
     <div class="cg-tip"><AnsiFormat @text="You need to reset your traits first!" /></div>

        <div class="action-buttons">
           <button name="reset" {{action 'resetTraits' char.name}} class="btn btn-secondary">Reset Traits</button>
        </div>     
    {{/if}}

   </div>

  <div role="tabpanel" class="tab-pane" id="systemgoals">
                      
         <div class="cg-tip"><AnsiFormat @text="You can set some character goals here. It is not mandatory, and goals can be added or edited even after getting approved." /></div>
         <br>
         <p> <b>Character Goals:</b> </p>
          <MarkdownEditor @text={{char.custom.goals}} />
 </div>

 <div role="tabpanel" class="tab-pane" id="special">
         <div class="cg-tip"><AnsiFormat @text="Some concepts need to set additional information in the space below. Please note: You may have to save your changes and refresh the page before these additional fields will show." /></div>
         {{#if char.custom.cg_adept}}
         <hr>
         <div class="cg-tip"><AnsiFormat @text="Adept concepts need to set the current state of progress on their marque here." /></div>
         <br>
         <p> <b>Marque Status (%):</b> </p>
         <Input @type="integer" @size={{25}} @value={{char.custom.marque}} />
         <br>
         {{/if}}
         {{#if char.custom.cg_connections}}
         <hr>
         <div class="cg-tip"><AnsiFormat @text="As you took the advantage connections, please specify these connections below, along with a brief explanation." /></div>
         <br>
         <p> <b>Connections:</b> </p>
         <MarkdownEditor @text={{char.custom.connections}} />
         {{/if}}
</div>
